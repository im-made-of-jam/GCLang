# command line build text
# ./Built/GCLang ./Built/b.gc -o ./bGCOut -I -D -DD -DDD

0&
puts{
    # first dereference
    <

    (
        # break on all letters printed
        dup 0 = if
            break
        endif

        # one off pointer number
        1 -

        # swap char to print and pointer counter
        swap

        putc
    )

    # drop pointer number from the stack
    drop
}

"cls" system

20 exit